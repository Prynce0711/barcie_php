<?php

declare(strict_types=1);

/* ---------------------------
   ADMIN: delete user account
   --------------------------- */
if ($action === 'admin_delete_user') {
    // Keep this explicit until user table model is finalized.
    handleResponse('Delete user action is currently unavailable in this endpoint.', false);
}
